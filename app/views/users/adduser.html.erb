<h1>Listing users</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @user.name %></td>
        <td><%= @user.address %></td>
        <td><%= link_to 'Show', @user %></td>
        <td><%= link_to 'Add friend', friendships_path(:friendship => { :user_id => current_user.id, :friend_id => @user.id } ), method: :post %></td>
      </tr>
  </tbody>
</table>

<br>
